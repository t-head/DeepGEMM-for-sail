# deepgemm CI 多版本组合矩阵参数
# 抽象出 HGGC * Torch * SDK 多版本组合的可调参数
# 由 checkin_test.yaml 通过 !include 引用,实现多流水线共享同一矩阵定义

product:
  description: '产品, 默认ppu'
  required: false
  type: string
  options: ['ppu', 'ppu.2v1_opensource']
  default: 'ppu.2v1_opensource'
sdk_type:
  description: 'sdk类型, 默认release'
  required: false
  type: string
  options: ['release', 'daily']
  default: 'daily'
hggc_versions:
  description: '多个hggc版本(仅作为可选项展示,实际矩阵以 version_combos 为准）'
  required: false
  type: array
  options: ['12.6.0', '12.8.0', '12.9.0', '13.0.0']
  default: ['13.0.0']
torch_versions:
  description: '多个torch版本(仅作为可选项展示,实际矩阵以 version_combos 为准）'
  required: false
  type: array
  options: ['2.6.0', '2.8.0', '2.9.0', '2.10.0']
  default: ['2.9.0']
sdk_versions:
  description: '多个sdk版本(仅作为可选项展示,实际矩阵以 version_combos 为准）'
  required: false
  type: array
  options: ["2.1.0_preview", "2.1.0"]
  default: ['2.1.0']
version_combos:
  description: 'HGGC * Torch * SDK 固定组合矩阵;每条对应一次完整编译测试。aone-ci 渲染为对象字符串,acbv_ci.yaml parse_combo 步骤负责解析'
  required: false
  type: array
  default:
    - hggc_version: '12.8.0'
      torch_version: '2.8.0'
      sdk_version: '2.1.0'
    - hggc_version: '13.0.0'
      torch_version: '2.9.0'
      sdk_version: '2.1.0'
sdk_daily_version:
  description: 'sdk版本, 默认latest'
  required: false
  type: string
  default: 'latest'
torch_daily_versions:
  description: 'torch daily版本号'
  required: false
  type: string
  default: 'latest'

